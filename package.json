{
    "name": "@mkrause/lifecycle-loader",
    "version": "0.2.1",
    "author": "mkrause",
    "license": "MIT",
    "repository": "https://github.com/mkrause/lifecycle-loader",
    "description": "Utilities for managing the state lifecycle of async loaded resources",
    "keywords": [],
    "files": [
        "src/",
        "lib-cjs/",
        "lib-esm/",
        "typings/"
    ],
    "typings": "./typings/index.d.ts",
    "engines": {
        "node": ">= 10.13"
    },
    "main": "lib-cjs/index.js",
    "module": "lib-esm/index.js",
    "scripts": {
        "build:cjs": "NODE_ENV=production BABEL_ENV=cjs babel src --out-dir lib-cjs --delete-dir-on-start --extensions '.js,.ts' --out-file-extension=.js",
        "build:esm": "NODE_ENV=production BABEL_ENV=esm babel src --out-dir lib-esm --delete-dir-on-start --extensions '.js,.ts' --out-file-extension=.js",
        "build:types": "tsc --project ./tsconfig.decl.json",
        "check-types": "tsc",
        "test:unit": "npm run build:esm && NODE_ENV=development BABEL_ENV=cjs mocha --require=@babel/register --recursive tests",
        "test": "npm run test:unit && npm run check-types && echo '[tsc] success'",
        "prepublishOnly": "npm run build:esm && npm run build:cjs && npm run build:types"
    },
    "devDependencies": {
        "@babel/core": "^7.9.6",
        "@babel/register": "^7.9.0",
        "@babel/cli": "^7.8.4",
        "@babel/preset-env": "^7.9.6",
        "@babel/plugin-proposal-class-properties": "^7.8.3",
        "babel-plugin-transform-builtin-extend": "^1.1.2",
        "@babel/preset-typescript": "^7.9.0",
        "typescript": "^3.9.3",
        "expect-type": "^0.7.4",
        "@types/node": "^14.0.5",
        "mocha": "^7.2.0",
        "chai": "^4.2.0",
        "chai-as-promised": "^7.1.1",
        "sinon": "^9.0.0"
    },
    "dependencies": {
        "message-tag": "^0.5.1",
        "proxy-extend": "^1.2.1"
    }
}
